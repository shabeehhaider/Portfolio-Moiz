# Vimeo personal access token (server-only — never exposed to the browser).
# Generate one at https://developer.vimeo.com/apps with the "public" + "private" video scopes.
# The cached server route at /api/work uses this to fetch the archive.
NUXT_VIMEO_TOKEN=your_vimeo_access_token_here
